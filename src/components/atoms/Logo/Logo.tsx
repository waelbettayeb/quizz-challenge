import React, { ReactElement } from "react";
import { Display4 } from "baseui/typography";
interface Props {}

function Logo({}: Props): ReactElement {
  return <Display4>Quiz</Display4>;
}

export default Logo;
